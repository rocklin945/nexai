<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rocklin.nexai.mapper.ChatHistoryMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into chat_history
        (id,
        message,
        messageType,
        appId,
        userId)
        values
        (UUID_SHORT(),
        #{message},
        #{messageType},
        #{appId},
        #{userId})
    </insert>
    <select id="selectListByAppId" resultType="com.rocklin.nexai.model.entity.ChatHistory">
        select * from chat_history where appId = #{appId}
        order by createTime ASC
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>
    <select id="selectAppChatHistoryByCursor" resultType="com.rocklin.nexai.model.entity.ChatHistory">
        SELECT *
        FROM chat_history
        WHERE appId = #{req.appId}
        <if test="req.userId != null">
            AND userId = #{req.userId}
        </if>
        <if test="req.lastCreateTime != null">
            AND createTime &lt; #{req.lastCreateTime}
        </if>
        <if test="req.messageType != null">
            AND messageType = #{req.messageType}
        </if>
        ORDER BY createTime DESC
        LIMIT #{req.pageSize}
    </select>
</mapper>